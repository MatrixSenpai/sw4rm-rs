#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct IntegrationApplicationResponse {
    pub coverimage: !,
    pub name: !,
    pub icon: !,
    pub id: !,
    pub description: !,
}
