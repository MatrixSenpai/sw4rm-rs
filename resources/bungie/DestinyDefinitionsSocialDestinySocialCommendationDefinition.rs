#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsSocialDestinySocialCommendationDefinition {
    pub index: !,
    pub activity_giving_limit: !,
    pub hash: !,
    pub parent_commendation_node_hash: !,
    pub redacted: !,
    pub display_priority: !,
    pub card_image_path: !,
}
