#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsRecordsDestinyRecordDefinition {
    pub for_title_gilding: !,
    pub presentation_node_type: !,
    pub redacted: !,
    pub record_value_style: !,
    pub scope: !,
    pub lore_hash: !,
    pub should_show_large_icons: !,
    pub hash: !,
    pub index: !,
}
