#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsPresentationDestinyPresentationNodeDefinition {
    pub original_icon: !,
    pub completion_record_hash: !,
    pub scope: !,
    pub disable_child_subscreen_navigation: !,
    pub index: !,
    pub objective_hash: !,
    pub node_type: !,
    pub screen_style: !,
    pub display_style: !,
    pub root_view_icon: !,
    pub hash: !,
    pub redacted: !,
    pub presentation_node_type: !,
    pub max_category_record_score: !,
}
