#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct TrendingTrendingEntryCommunityCreation {
    pub post_id: !,
    pub body: !,
    pub upvotes: !,
    pub media: !,
    pub title: !,
    pub author: !,
    pub author_membership_id: !,
}
