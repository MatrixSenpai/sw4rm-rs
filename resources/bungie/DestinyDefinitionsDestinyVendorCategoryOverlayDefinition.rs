#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyVendorCategoryOverlayDefinition {
    pub icon: !,
    pub currency_item_hash: !,
    pub description: !,
    pub title: !,
    pub choice_description: !,
}
