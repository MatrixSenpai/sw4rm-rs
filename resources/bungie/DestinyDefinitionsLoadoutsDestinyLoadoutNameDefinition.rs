#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsLoadoutsDestinyLoadoutNameDefinition {
    pub hash: !,
    pub index: !,
    pub name: !,
    pub redacted: !,
}
