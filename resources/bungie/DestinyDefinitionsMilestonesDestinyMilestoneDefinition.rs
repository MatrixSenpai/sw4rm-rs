#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsMilestonesDestinyMilestoneDefinition {
    pub show_in_explorer: !,
    pub vendors_display_title: !,
    pub show_in_milestones: !,
    pub image: !,
    pub recruitable: !,
    pub index: !,
    pub display_preference: !,
    pub friendly_name: !,
    pub redacted: !,
    pub has_predictable_dates: !,
    pub is_in_game_milestone: !,
    pub default_order: !,
    pub explore_prioritizes_activity_image: !,
    pub hash: !,
    pub milestone_type: !,
}
