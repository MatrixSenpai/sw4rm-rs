#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsPresentationDestinyScoredPresentationNodeBaseDefinition {
    pub redacted: !,
    pub hash: !,
    pub presentation_node_type: !,
    pub max_category_record_score: !,
    pub index: !,
}
