#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyProgressionStepDefinition {
    pub icon: !,
    pub display_effect_type: !,
    pub progress_total: !,
    pub step_name: !,
}
