#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyStatDefinition {
    pub redacted: !,
    pub hash: !,
    pub index: !,
    pub aggregation_type: !,
    pub has_computed_block: !,
    pub stat_category: !,
}
