#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct GroupsVtwoGroupPotentialMembershipSearchResponse {
    pub replacement_continuation_token: !,
    pub has_more: !,
    pub total_results: !,
    pub use_total_results: !,
}
