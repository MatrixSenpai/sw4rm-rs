#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsSocialDestinySocialCommendationNodeDefinition {
    pub hash: !,
    pub index: !,
    pub parent_commendation_node_hash: !,
    pub redacted: !,
}
