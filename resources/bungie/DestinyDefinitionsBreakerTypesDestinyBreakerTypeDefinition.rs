#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsBreakerTypesDestinyBreakerTypeDefinition {
    pub hash: !,
    pub redacted: !,
    pub enum_value: !,
    pub index: !,
}
