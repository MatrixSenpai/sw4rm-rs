#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsMetricsDestinyMetricDefinition {
    pub redacted: !,
    pub index: !,
    pub tracking_objective_hash: !,
    pub lower_value_is_better: !,
    pub hash: !,
    pub presentation_node_type: !,
}
