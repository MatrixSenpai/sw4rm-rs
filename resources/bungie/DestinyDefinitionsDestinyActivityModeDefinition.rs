#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyActivityModeDefinition {
    pub index: !,
    pub redacted: !,
    pub activity_mode_category: !,
    pub is_team_based: !,
    pub mode_type: !,
    pub pgcr_image: !,
    pub display: !,
    pub is_aggregate_mode: !,
    pub hash: !,
    pub order: !,
    pub friendly_name: !,
}
