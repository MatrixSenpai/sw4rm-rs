#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyDamageTypeDefinition {
    pub transparent_icon_path: !,
    pub show_icon: !,
    pub hash: !,
    pub enum_value: !,
    pub index: !,
    pub redacted: !,
}
