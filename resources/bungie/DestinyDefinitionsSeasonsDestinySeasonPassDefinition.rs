#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsSeasonsDestinySeasonPassDefinition {
    pub prestige_progression_hash: !,
    pub redacted: !,
    pub index: !,
    pub reward_progression_hash: !,
    pub hash: !,
}
