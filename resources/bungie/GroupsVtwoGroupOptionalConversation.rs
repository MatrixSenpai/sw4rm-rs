#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct GroupsVtwoGroupOptionalConversation {
    pub chat_security: !,
    pub group_id: !,
    pub conversation_id: !,
    pub chat_enabled: !,
    pub chat_name: !,
}
