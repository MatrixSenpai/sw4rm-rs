#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyComponentsPresentationDestinyPresentationNodeComponent {
    pub record_category_score: !,
    pub state: !,
    pub completion_value: !,
    pub progress_value: !,
}
