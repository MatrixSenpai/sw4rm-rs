#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct TrendingTrendingEntry {
    pub feature_image: !,
    pub is_featured: !,
    pub image: !,
    pub end_date: !,
    pub identifier: !,
    pub link: !,
    pub mpfour_video: !,
    pub weight: !,
    pub webm_video: !,
    pub start_date: !,
    pub display_name: !,
    pub tagline: !,
    pub entity_type: !,
    pub creation_date: !,
}
