#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyTalentGridDefinition {
    pub index: !,
    pub redacted: !,
    pub max_grid_level: !,
    pub hash: !,
    pub grid_level_per_column: !,
    pub progression_hash: !,
}
