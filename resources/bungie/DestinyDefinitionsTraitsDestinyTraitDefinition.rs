#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsTraitsDestinyTraitDefinition {
    pub hash: !,
    pub index: !,
    pub display_hint: !,
    pub redacted: !,
}
