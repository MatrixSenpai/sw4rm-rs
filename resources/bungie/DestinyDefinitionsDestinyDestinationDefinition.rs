#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyDestinationDefinition {
    pub index: !,
    pub place_hash: !,
    pub hash: !,
    pub default_freeroam_activity_hash: !,
    pub redacted: !,
}
