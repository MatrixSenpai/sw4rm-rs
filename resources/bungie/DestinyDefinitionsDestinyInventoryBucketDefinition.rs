#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyInventoryBucketDefinition {
    pub scope: !,
    pub index: !,
    pub redacted: !,
    pub has_transfer_destination: !,
    pub hash: !,
    pub category: !,
    pub bucket_order: !,
    pub item_count: !,
    pub enabled: !,
    pub fifo: !,
    pub location: !,
}
