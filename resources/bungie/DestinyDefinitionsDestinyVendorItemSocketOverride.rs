#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyVendorItemSocketOverride {
    pub randomized_options_count: !,
    pub socket_type_hash: !,
    pub single_item_hash: !,
}
