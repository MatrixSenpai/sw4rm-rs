#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyProgressionDefinition {
    pub rank_icon: !,
    pub redacted: !,
    pub hash: !,
    pub scope: !,
    pub repeat_last_step: !,
    pub faction_hash: !,
    pub source: !,
    pub index: !,
    pub visible: !,
}
