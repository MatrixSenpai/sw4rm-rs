#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyFactionDefinition {
    pub reward_item_hash: !,
    pub index: !,
    pub reward_vendor_hash: !,
    pub redacted: !,
    pub progression_hash: !,
    pub hash: !,
}
