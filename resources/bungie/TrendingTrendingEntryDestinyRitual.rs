#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct TrendingTrendingEntryDestinyRitual {
    pub date_start: !,
    pub image: !,
    pub icon: !,
    pub title: !,
    pub date_end: !,
    pub subtitle: !,
}
