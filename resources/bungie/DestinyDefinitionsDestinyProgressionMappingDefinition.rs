#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsDestinyProgressionMappingDefinition {
    pub display_units: !,
    pub redacted: !,
    pub index: !,
    pub hash: !,
}
