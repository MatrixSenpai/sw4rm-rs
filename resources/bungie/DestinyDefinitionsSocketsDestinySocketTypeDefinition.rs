#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsSocketsDestinySocketTypeDefinition {
    pub overrides_ui_appearance: !,
    pub hash: !,
    pub index: !,
    pub is_preview_enabled: !,
    pub hide_duplicate_reusable_plugs: !,
    pub avoid_duplicates_on_initialization: !,
    pub socket_category_hash: !,
    pub redacted: !,
    pub always_randomize_sockets: !,
    pub visibility: !,
}
