#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
pub struct DestinyDefinitionsPresentationDestinyPresentationNodeBaseDefinition {
    pub hash: !,
    pub presentation_node_type: !,
    pub index: !,
    pub redacted: !,
}
