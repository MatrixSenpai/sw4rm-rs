mod model;
mod file;
mod import;
mod attribute;
mod field;

use sw4rm_rs::*;

pub struct GenerationOptions {
    pub parse_models: bool,
    pub parse_apis: bool,
}

pub fn parse_spec(spec: Spec, options: GenerationOptions) {

}

struct Representation {

}
